#!/usr/bin/env lua

bigNum=99933399922255511
--this is comment 
-- you need to have comma in order to use variable 
io.write(" the number is ",bigNum,"\n"  )
--every type is trated the same in lua
-- to prove it - we can check it with type() function : --> 
io.write("the type is ", type(bigNum) ,"\n ")
